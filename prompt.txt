No need to read .env, it is already configured.
I have already configured my Paddle dashboard and settings. Please update my application code so the /pay route bypasses the 'logged-in user redirect to dashboard' middleware, loads Paddle.js, and automatically opens Paddle.Checkout.open({ transactionId: txnId }) when ?_ptxn= is present in the URL query string. And, if user try to do something before buying a plan, redirect to settings & billing page instead of just showing a notification pop-up. And, user should be able to navigate settings before choosing plan via mobile dropdown nav too.