No need to read .env, it is already configured.
I am building a Node.js (backend) + React (frontend) application with OAuth 2.0 integration for Xero. 

QuickBooks OAuth works fine with my callback URL (https://eronflow.top/api/oauth/callback), but Xero consistently throws an "Invalid Redirect URI" error on login.xero.com.

My Xero Configuration:
- Client ID: B22CE84C20A8434D8B58C993E21746F5
- App Type: Web App
- Redirect URI in Xero Portal: https://eronflow.top/api/oauth/callback

Please review my backend route that generates the Xero authorization URL and fix the following issues:

1. Scope Formatting: Ensure spaces between scopes are properly URL-encoded as %20 (e.g. using URLSearchParams or encodeURIComponent).
2. Required Identity Scopes: Ensure 'openid', 'profile', and 'email' are included in the scopes array alongside 'accounting.transactions', 'accounting.contacts', and 'offline_access'.
3. Web App Flow: Since this is a confidential Web App using a Client Secret, ensure PKCE parameters (code_challenge / code_challenge_method) are removed unless strictly required, or ensure they are correctly generated if PKCE is intended.
4. Exact Redirect URI Match: Make sure the exact string 'https://eronflow.top/api/oauth/callback' (without extra slashes or misencoded characters) is passed to Xero.

Please provide:
1. The updated Node.js endpoint/service code for generating the consent URL.
2. The callback endpoint code for exchanging the auth code for tokens.
3. Instructions on how the React frontend should trigger this login flow.